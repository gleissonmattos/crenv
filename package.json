{
  "name": "crenv",
  "version": "0.9.7",
  "description": "Performs the loading and reading of cross-side environment variables at runtime, on the server and in the browser, including generated static HTML pages.",
  "main": "src/index",
  "engine": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "lint:fix": "eslint --fix ."
  },
  "readmeFilename": "README.md",
  "author": {
    "name": "Gleisson Ricardo",
    "email": "contato.mattos@outlook.com"
  },
  "license": "MIT",
  "bin": {
    "crenv": "src/cli.js"
  },
  "dependencies": {
    "commander": "^8.3.0",
    "dotenv": "^10.0.0",
    "shelljs": "^0.8.4"
  }
}
